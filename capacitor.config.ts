import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.qihun.gomoku',
  appName: '棋魂五子棋',
  webDir: 'dist',
  backgroundColor: '#0d0f0f',
  android: {
    backgroundColor: '#0d0f0f',
  },
}

export default config
